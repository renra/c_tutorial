//Prevent double inclusion error
#ifndef CONSTANTS
  #define CONSTANTS 1
  #define HTTP_SUCCESS 200
  #define HTTP_INTERNAL_SERVER_ERROR 500
  #define max(x, y) ((x>y) ? x : y)


  void func() { return; }


#endif
